#include <iostream>
#include <string>
using namespace std;

int stringToNumber(char *number, int len) {
    

      
} 

int main(){
	char number[]  = "123456";
	int len = sizeof(number)/ sizof(char);
	stringToNumber(number, len);


	return 0;
}