#include <bits/stdc++.h>
using namespace std; 

int maxProfit(int price[], int len) {
	int profit[len] = {0};
	//take the last profit value as the maximum profit value
    int max_price = price[len -1];

   //

}